<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pyokemon.booking.repository.BookingRepository">

    <select id="findSeatIdsByEventScheduleId" resultType="long">
        SELECT seat_id
        FROM tb_booking
        WHERE event_schedule_id = #{eventScheduleId}
        AND status IN ('PENDING', 'BOOKED')
    </select>

    <select id="findSeatStatusInfosByEventScheduleId" resultType="com.pyokemon.booking.dto.response.SeatStatusInfo">
        SELECT seat_id as seatId, status
        FROM tb_booking
        WHERE event_schedule_id = #{eventScheduleId}
        AND status IN ('PENDING', 'BOOKED')
    </select>

    <select id="findByAccountId" resultType="Booking">
        SELECT id, event_schedule_id, seat_id, account_id, tenant_id, payment_id, status, created_at, updated_at
        FROM tb_booking
        WHERE account_id = #{accountId}
    </select>

    <select id="findAllByEventScheduleIdAndSeatId" resultType="Booking">
        SELECT id, event_schedule_id, seat_id, account_id, tenant_id, payment_id, status, created_at, updated_at
        FROM tb_booking
        WHERE event_schedule_id = #{eventScheduleId} AND seat_id = #{seatId}
        ORDER BY created_at DESC
    </select>

    <select id="findActiveBookingByEventScheduleIdAndAccountId" resultType="Booking">
        SELECT id, event_schedule_id, seat_id, account_id, tenant_id, payment_id, status, created_at, updated_at
        FROM tb_booking
        WHERE event_schedule_id = #{eventScheduleId} AND account_id = #{accountId}
        AND status IN ('PENDING', 'BOOKED')
        LIMIT 1
    </select>

    <select id="findById" resultType="Booking">
        SELECT id, event_schedule_id, seat_id, account_id, tenant_id, payment_id, status, created_at, updated_at
        FROM tb_booking
        WHERE id = #{id}
    </select>

    <select id="findPendingBookings" resultType="Booking">
        SELECT id, event_schedule_id, seat_id, account_id, tenant_id, payment_id, status, created_at, updated_at
        FROM tb_booking
        WHERE status = 'PENDING'
    </select>

    <select id="findPendingBookingsOlderThan" resultType="Booking">
        SELECT id, event_schedule_id, seat_id, account_id, tenant_id, payment_id, status, created_at, updated_at
        FROM tb_booking
        WHERE status = 'PENDING'
        AND created_at &lt; #{createdAt}
    </select>

    <select id="findValidBookingsWithEventInfo" resultType="com.pyokemon.booking.dto.response.ValidBookingDetail">
        SELECT 
            b.id as bookingId,
            b.event_schedule_id as eventScheduleId,
            b.tenant_id as tenantId
        FROM tb_booking b
        WHERE b.account_id = #{accountId}
        AND b.status = 'BOOKED'
        AND b.id IN
        <foreach collection="bookingIds" item="bookingId" open="(" separator="," close=")">
            #{bookingId}
        </foreach>
    </select>

    <insert id="save" parameterType="Booking" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_booking (event_schedule_id, seat_id, account_id, tenant_id, payment_id, status)
        VALUES (#{eventScheduleId}, #{seatId}, #{accountId}, #{tenantId}, #{paymentId}, #{status})
    </insert>

    <update id="update" parameterType="Booking">
        UPDATE tb_booking
        SET status = #{status}, payment_id = #{paymentId}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM tb_booking
        WHERE id = #{id}
    </delete>

    <update id="updateStatus">
        UPDATE tb_booking
        SET status = #{status}, updated_at = NOW()
        WHERE event_schedule_id = #{eventScheduleId}
        AND status = 'BOOKED'
    </update>

    <select id="findAllByEventScheduleId" parameterType="long" resultType="Booking">
        SELECT id, event_schedule_id, seat_id, account_id, tenant_id, status
        FROM tb_booking
        WHERE event_schedule_id = #{eventScheduleId}
        AND status = 'BOOKED'
    </select>

    <select id="findByEventScheduleIdAndStatus" resultType="Booking">
        SELECT id, event_schedule_id, seat_id, account_id, tenant_id, payment_id, status, created_at, updated_at
        FROM tb_booking
        WHERE event_schedule_id = #{eventScheduleId}
              AND status = #{status}
    </select>

</mapper> 