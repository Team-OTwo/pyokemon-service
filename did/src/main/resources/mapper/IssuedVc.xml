<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pyokemon.did.domain.repository.IssuedVcRepository">

    <resultMap id="IssuedVcResultMap" type="com.pyokemon.did.domain.IssuedVc">
        <id column="id" property="id" />
        <result column="credential_exchange_id" property="credentialExchangeId" />
        <result column="booking_id" property="bookingId" />
        <result column="tenant_id" property="tenantId" />
        <result column="credo_conn_id" property="credoConnId" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <select id="findById" resultMap="IssuedVcResultMap">
        SELECT * FROM tb_issued_vc WHERE id = #{id}
    </select>

    <select id="findByCredentialExchangeId" resultMap="IssuedVcResultMap">
        SELECT * FROM tb_issued_vc WHERE credential_exchange_id = #{credentialExchangeId}
    </select>

    <select id="findByBookingId" resultMap="IssuedVcResultMap">
        SELECT * FROM tb_issued_vc WHERE booking_id = #{bookingId} ORDER BY created_at DESC
    </select>

    <select id="findByTenantId" resultMap="IssuedVcResultMap">
        SELECT * FROM tb_issued_vc WHERE tenant_id = #{tenantId} ORDER BY created_at DESC
    </select>

    <select id="findByCredoConnId" resultMap="IssuedVcResultMap">
        SELECT * FROM tb_issued_vc WHERE credo_conn_id = #{credoConnId} ORDER BY created_at DESC
    </select>

    <select id="findByStatus" resultMap="IssuedVcResultMap">
        SELECT * FROM tb_issued_vc WHERE status = #{status} ORDER BY created_at DESC
    </select>

    <select id="findByTenantIdAndStatus" resultMap="IssuedVcResultMap">
        SELECT * FROM tb_issued_vc WHERE tenant_id = #{tenantId} AND status = #{status} ORDER BY created_at DESC
    </select>

    <select id="findByBookingIdAndStatus" resultMap="IssuedVcResultMap">
        SELECT * FROM tb_issued_vc WHERE booking_id = #{bookingId} AND status = #{status} ORDER BY created_at DESC LIMIT 1
    </select>

    <insert id="save" parameterType="com.pyokemon.did.domain.IssuedVc" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_issued_vc (credential_exchange_id, booking_id, tenant_id, credo_conn_id, status, created_at, updated_at)
        VALUES (#{credentialExchangeId}, #{bookingId}, #{tenantId}, #{credoConnId}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.pyokemon.did.domain.IssuedVc">
        UPDATE tb_issued_vc 
        SET credential_exchange_id = #{credentialExchangeId}, booking_id = #{bookingId}, tenant_id = #{tenantId}, credo_conn_id = #{credoConnId}, status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM tb_issued_vc WHERE id = #{id}
    </delete>

    <delete id="deleteByCredentialExchangeId">
        DELETE FROM tb_issued_vc WHERE credential_exchange_id = #{credentialExchangeId}
    </delete>

    <delete id="deleteByBookingId">
        DELETE FROM tb_issued_vc WHERE booking_id = #{bookingId}
    </delete>

</mapper> 