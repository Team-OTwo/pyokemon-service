<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pyokemon.did.domain.repository.AcaPyConnectionRepository">

    <resultMap id="AcaPyConnectionResultMap" type="com.pyokemon.did.domain.AcaPyConnection">
        <id column="id" property="id" />
        <result column="connection_id" property="connectionId" />
        <result column="invi_msg_id" property="inviMsgId" />
        <result column="tenant_id" property="tenantId" />
        <result column="user_id" property="userId" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <select id="findByTenantIdAndUserId" resultMap="AcaPyConnectionResultMap">
        SELECT * FROM tb_acapy_connection WHERE tenant_id = #{tenantId} AND user_id = #{userId} ORDER BY created_at DESC
    </select>
    
    <select id="existsByTenantIdAndUserId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM tb_acapy_connection WHERE tenant_id = #{tenantId} AND user_id = #{userId} AND status = 'ACTIVE'
    </select>

    <insert id="save" parameterType="com.pyokemon.did.domain.AcaPyConnection" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_acapy_connection (connection_id, invi_msg_id, tenant_id, user_id, status, created_at, updated_at)
        VALUES (#{connectionId}, #{inviMsgId}, #{tenantId}, #{userId}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.pyokemon.did.domain.AcaPyConnection">
        UPDATE tb_acapy_connection 
        SET connection_id = #{connectionId}, invi_msg_id = #{inviMsgId}, tenant_id = #{tenantId}, user_id = #{userId}, status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM tb_acapy_connection WHERE id = #{id}
    </delete>

</mapper> 