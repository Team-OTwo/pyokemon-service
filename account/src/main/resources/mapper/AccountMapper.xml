<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pyokemon.account.auth.repository.AccountRepository">

    <select id="findByLoginId" resultType="com.pyokemon.account.auth.entity.Account">
        SELECT * FROM tb_account WHERE login_id = #{loginId}
    </select>

    <select id="findByAccountId" resultType="com.pyokemon.account.auth.entity.Account">
        SELECT * FROM tb_account WHERE account_id = #{accountId}
    </select>

    <insert id="insert" parameterType="com.pyokemon.account.auth.entity.Account" useGeneratedKeys="true" keyProperty="accountId">
        INSERT INTO tb_account (role, login_id, password, status)
        VALUES (#{role}, #{loginId}, #{password}, #{status})
    </insert>

    <update id="update" parameterType="com.pyokemon.account.auth.entity.Account">
        UPDATE tb_account 
        SET role = #{role}, login_id = #{loginId}, password = #{password}, status = #{status}
        WHERE account_id = #{accountId}
    </update>

    <update id="updateStatus">
        UPDATE tb_account SET status = #{status} WHERE account_id = #{accountId}
    </update>

    <update id="updatePassword">
        UPDATE tb_account SET password = #{password} WHERE account_id = #{accountId}
    </update>

    <select id="existsByLoginId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM tb_account WHERE login_id = #{loginId}
    </select>

</mapper> 