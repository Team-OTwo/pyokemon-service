<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pyokemon.event.repository.EventRepository">
    <insert id="save" parameterType="Event" useGeneratedKeys="true" keyProperty="eventId" keyColumn="event_id">
        INSERT INTO tb_event (
        account_id,
        title,
        age_limit,
        description,
        genre,
        thumbnail_url,
        status,
        created_at,
        updated_at
        ) VALUES (
        #{accountId},
        #{title},
        #{ageLimit},
        #{description},
        #{genre},
        #{thumbnailUrl},
        #{status},
        #{createdAt},
        #{updatedAt}
        )
        <selectKey resultType="long" keyProperty="eventId" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <select id="findEventDetailByEventId" resultType="EventDetailResponseDTO">
        SELECT
        e.event_id,
        e.title,
        e.age_limit,
        e.description,
        e.genre,
        e.thumbnail_url,
        es.event_schedule_id,
        es.ticket_open_at,
        es.event_date,
        v.venue_name,
        e.account_id as tenant_id
        FROM tb_event e
        JOIN tb_event_schedule es ON es.event_id = e.event_id
        JOIN tb_venue v ON es.venue_id = v.venue_id

        WHERE e.event_id = #{event_id}
    </select>

    <select id="findSeatPriceByEventScheduleId" resultType="SeatPriceResponseDto">
        SELECT class_name, price
        FROM tb_price p JOIN tb_seat_class sc
        ON p.seat_class_id = sc.seat_class_id
        WHERE event_schedule_id = #{eventScheduleId};
    </select>


    <!--    bff-->
    <select id="findBffEventById" resultType="BffEventDto">
        SELECT event_id as id, title, thumbnail_url
        FROM tb_event
        WHERE event_id = #{eventId}
    </select>
</mapper>