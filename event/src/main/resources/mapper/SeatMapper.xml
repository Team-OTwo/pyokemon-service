<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pyokemon.event.repository.SeatRepository">

    <select id="findBySeatClassId" resultType="Seat" parameterType="long">
        SELECT seat_id, venue_id, seat_class_id, floor, `row`, col, created_at, updated_at
        FROM tb_seat
        WHERE seat_class_id = #{param1}
    </select>

    <select id="findByVenueIdAndSeatClassId" resultType="Seat">
        SELECT seat_id, venue_id, seat_class_id, floor, `row`, col, created_at, updated_at
        FROM tb_seat
        WHERE venue_id = #{param1} AND seat_class_id = #{param2}
    </select>

    <select id="countByVenueIdAndSeatClassId" resultType="long">
        SELECT COUNT(*)
        FROM tb_seat
        WHERE venue_id = #{param1} AND seat_class_id = #{param2}
    </select>

    <select id="findByVenueId" resultType="Seat" parameterType="long">
        SELECT seat_id, venue_id, seat_class_id, floor, `row`, col, created_at, updated_at
        FROM tb_seat
        WHERE venue_id = #{param1}
    </select>

    <select id="findSeatById" resultType="com.pyokemon.event.dto.SeatInfoDto">
        SELECT seat_id, seat_class_id, floor, `row`, col FROM tb_seat WHERE seat_id = #{seatId}
    </select>

    <!-- 배치: ids가 비면 0행 반환 -->
    <select id="findSeatsByIdIn"
            parameterType="map"
            resultType="com.pyokemon.event.dto.SeatInfoDto">
        SELECT
        seat_id,
        seat_class_id,
        floor,
        `row`,
        col
        FROM tb_seat
        WHERE
        <choose>
            <when test="ids != null and ids.size() > 0">
                seat_id IN
                <foreach collection="ids" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </when>
            <otherwise>
                1 = 0
            </otherwise>
        </choose>
    </select>

</mapper>
