<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pyokemon.event.repository.SeatClassRepository">

    <select id="findAll" resultType="SeatClass">
        SELECT seat_class_id, class_name, created_at, updated_at
        FROM tb_seat_class
    </select>

    <select id="findById" resultType="SeatClass" parameterType="long">
        SELECT seat_class_id, class_name, created_at, updated_at
        FROM tb_seat_class
        WHERE seat_class_id = #{param1}
    </select>

</mapper>
