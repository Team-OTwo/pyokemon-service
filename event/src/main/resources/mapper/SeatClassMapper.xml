<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pyokemon.event.repository.SeatClassRepository">

    <select id="findByClassName" resultType="SeatClass" parameterType="string">
        SELECT seat_class_id, class_name, priority, created_at, updated_at
        FROM tb_seat_class
        WHERE class_name = #{param1}
    </select>

    <select id="findAll" resultType="SeatClass">
        SELECT seat_class_id, class_name, priority, created_at, updated_at
        FROM tb_seat_class
        ORDER BY priority ASC
    </select>

    <select id="findByVenueId" resultType="SeatClass" parameterType="long">
        SELECT DISTINCT sc.seat_class_id, sc.class_name, sc.priority, sc.created_at, sc.updated_at
        FROM tb_seat_class sc
        INNER JOIN tb_seat s ON sc.seat_class_id = s.seat_class_id
        WHERE s.venue_id = #{param1}
        ORDER BY sc.priority ASC
    </select>

    <select id="findById" resultType="SeatClass" parameterType="long">
        SELECT seat_class_id, class_name, priority, created_at, updated_at
        FROM tb_seat_class
        WHERE seat_class_id = #{param1}
    </select>

    <select id="findSeatClassById" resultType="com.pyokemon.event.dto.SeatClassInfoDto">
        SELECT seat_class_id, class_name, priority FROM tb_seat_class WHERE seat_class_id = #{seatClassId}
    </select>

    <!-- 배치 조회 -->
    <select id="findSeatClassesByIdIn"
            parameterType="map"
            resultType="com.pyokemon.event.dto.SeatClassInfoDto">
        SELECT
        seat_class_id,
        class_name,
        priority
        FROM tb_seat_class
        WHERE
        <choose>
            <when test="ids != null and ids.size() > 0">
                seat_class_id IN
                <foreach collection="ids" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </when>
            <otherwise>
                1 = 0
            </otherwise>
        </choose>
    </select>

</mapper>
