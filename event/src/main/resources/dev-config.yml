spring:
  datasource:
    url: jdbc:mysql://pyokemon-event-mysql:3306/pyokemon_event?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
  data:
    redis:
      host: pyokemon-redis
      port: 6379
  kafka:
    bootstrap-servers: pyokemon-kafka:29092